<section class="course-admin course-admin--form">
  <div class="course-admin__shell course-admin__shell--form">
    <header class="course-admin__header course-admin__header--form">
      <div>
        <p class="course-admin__eyebrow">
          <%= teacher.persisted? ? "Update Teacher" : "Create Teacher" %>
        </p>
        <h1 class="course-admin__title">
          <%= teacher.persisted? ? "Edit Teacher" : "Add Teacher" %>
        </h1>
        <p class="course-admin__subtitle">
          Keep profile details complete so course assignments and communication stay accurate.
        </p>
      </div>
      <%= link_to "Back to Teachers", admin_teachers_path, class: "course-admin__ghost-link" %>
    </header>

    <div class="course-admin__form-shell">
      <%= form_for [:admin, teacher], html: { class: "course-admin__form" } do |f| %>
        <%= render "shared/error", model: teacher %>

        <div class="course-admin__field">
          <%= f.label :name, "Name", class: "course-admin__label" %>
          <%= f.text_field :name, placeholder: "Enter full name", class: "course-admin__input" %>
        </div>

        <div class="course-admin__field">
          <%= f.label :father_name, "Father Name", class: "course-admin__label" %>
          <%= f.text_field :father_name, placeholder: "Enter father name", class: "course-admin__input" %>
        </div>

        <div class="course-admin__field">
          <%= f.label :email, "Email", class: "course-admin__label" %>
          <%= f.email_field :email, autocomplete: "email", placeholder: "name@example.com", class: "course-admin__input" %>
        </div>

        <div class="course-admin__field">
          <%= f.label :phone_number, "Phone Number", class: "course-admin__label" %>
          <%= f.telephone_field :phone_number, placeholder: "03XXXXXXXXX", class: "course-admin__input" %>
        </div>

        <div class="course-admin__field">
          <%= f.label :password, "Password", class: "course-admin__label" %>
          <%= f.password_field :password, autocomplete: "new-password", class: "course-admin__input" %>
          <% if @minimum_password_length %>
            <span class="course-admin__meta"><%= @minimum_password_length %> characters minimum</span>
          <% end %>
        </div>

        <div class="course-admin__field">
          <%= f.label :password_confirmation, "Confirm Password", class: "course-admin__label" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "course-admin__input" %>
        </div>

        <div class="course-admin__field">
          <%= f.label :dob, "Date of Birth", class: "course-admin__label" %>
          <%= f.date_field :dob, class: "course-admin__input" %>
        </div>

        <div class="course-admin__field">
          <%= f.label :address, "Address", class: "course-admin__label" %>
          <%= f.text_field :address, placeholder: "Enter address", class: "course-admin__input" %>
        </div>

        <div class="course-admin__field">
          <%= f.label :course_ids, "Expertise", class: "course-admin__label" %>
          <% selected_course_ids = teacher.course_ids.map(&:to_s) %>
          <%= hidden_field_tag "course_ids[]", "" %>
          <details class="course-admin__multi-select">
            <summary class="course-admin__multi-select-trigger">
              <span>
                <%= selected_course_ids.any? ? "#{selected_course_ids.size} course(s) selected" : "Select expertise courses" %>
              </span>
              <span class="course-admin__multi-select-chevron" aria-hidden="true"></span>
            </summary>
            <div class="course-admin__multi-select-menu">
              <% Course.order(:name).each do |course| %>
                <label class="course-admin__multi-option">
                  <%= check_box_tag "course_ids[]", course.id, selected_course_ids.include?(course.id.to_s), class: "course-admin__multi-checkbox" %>
                  <span><%= course.name %></span>
                </label>
              <% end %>
            </div>
          </details>
          <span class="course-admin__meta">Open the dropdown and select one or more courses.</span>
        </div>

        <div class="course-admin__form-actions">
          <%= link_to "Cancel", admin_teachers_path, class: "course-admin__secondary-button" %>
          <%= f.submit(teacher.persisted? ? "Save Changes" : "Create Teacher", class: "course-admin__primary-button") %>
        </div>
      <% end %>
    </div>
  </div>
</section>
